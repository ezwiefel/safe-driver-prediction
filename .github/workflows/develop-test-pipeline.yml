name: Run Pipeline Test | Development

on: 
  pull_request:
    branches:
      - main

jobs:
  test-pipeline:
    uses: ezwiefel/safe-driver-prediction/.github/workflows/pipeline.yml
    with:
      environment: development
    secrets:
      azure_sp_creds: ${{ secrets.AZURE_SP_CREDS }}


